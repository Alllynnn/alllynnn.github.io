{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm install -g hexo-cli && npm install && hexo generate",
        "outputDirectory": "public"
      }
    }
  ]
} 